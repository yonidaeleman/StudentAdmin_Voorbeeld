spring:
  datasource:
    username: yoni
    password:
    url: jdbc:h2:mem:Administartion
  h2:
    console:
      enabled: true
      path: /admin
  sql:
    init:
      mode: always
  jpa:
    hibernate:
      ddl-auto: none
    database-platform: org.hibernate.dialect.H2Dialect
    show-sql: true
